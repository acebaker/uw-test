<!DOCTYPE html>
<html>
	<head>
	  	<title><%= title %></title>
		<%= stylesheet_link_tag :all %>
		<%= javascript_include_tag :all %>
		<script>
			$("img").lazyload({
			     placeholder : "general/white.jpg",       
			     effect      : "fadeIn"
			 });
		</script>
		<%= csrf_meta_tag %>
	</head>
	<body>
		<%= render 'layouts/header' %>
		<div id="content">
			<%= yield %>
		</div> <!-- end content -->
	</body>
</html>
